<div class="row">
  <div class="span6 offset3 col-md-6 col-md-offset-3">
  	<% @users.each do |i| %>
  		<%= gravatar_for i %>
  		<%= i.first_name %>
  		<%= i.last_name %><br>
  	<% end %>
  </div>
</div>

